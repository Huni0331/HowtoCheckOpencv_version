dear someone
hi
============usr_info=============================================
git config -l // 
git config --global user.email "이메일"
git config --global user.name "이름"
=================================================================
git init // make ".git" or reset ".git"

<how to upload git_file>
1. git add <filename.file_extension>

2. git commit -m "something"

3. git push
=================================================================

<how to delete git_file>
1. git rm --cached <filename.file_extension>

2. git commit -m "something"

3. git push
=================================================================

<how to make git_branch>
1. git branch "branch_name"

=================================================================

<how to move_git_branch>
1. git checkout "branch_name"

=================================================================

<how to delete git_branch>
1. git branch -d "branch_name"
 1-1) git branch -a //check branch

2. git push origin :"branch_name"



